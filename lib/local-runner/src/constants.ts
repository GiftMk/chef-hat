import path from 'node:path'

const ROOT_DIRECTORY = 'local-runner'
export const INPUTS_DIRECTORY = path.join(ROOT_DIRECTORY, 'inputs')
export const OUTPUTS_DIRECTORY = path.join(ROOT_DIRECTORY, 'outputs')
